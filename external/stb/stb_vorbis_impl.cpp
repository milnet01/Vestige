/// @file stb_vorbis_impl.cpp
/// @brief Implementation compilation unit for stb_vorbis OGG decoder.
///
/// stb_vorbis.c is a single-file library. Including it without
/// STB_VORBIS_HEADER_ONLY compiles the full implementation.
/// Other TUs include it with STB_VORBIS_HEADER_ONLY to get declarations only.
#include "stb_vorbis.c"
