{
  "name": "vestige-cursor-bridge-mcp",
  "version": "0.1.0",
  "description": "MCP server that forwards ide_* tool calls to the Vestige Cursor Bridge VS Code extension.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -p ./ --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
